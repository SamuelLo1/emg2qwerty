# @package _global_
datamodule:
  _target_: emg2qwerty.tuning.SubsampledWindowedEMGDataModule
  window_length: 8000
  padding: [1800, 200]
  # Use sensible defaults here to avoid CLI interpolation parsing issues.
  batch_size: 32
  num_workers: 4
  train_fraction: 1.0
